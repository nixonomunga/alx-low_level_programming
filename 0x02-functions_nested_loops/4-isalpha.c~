/*
 * File name: 4-isalpha.c
 *
 * Made by: Nixon
 */

#include "main.h"

/**
 * _isalpha - checks letter
 *
 * Return: 1 if successful
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}

	else

	{
		return (0);
	}
}
