/**
 * _strlen - returns length of a string
 *
 * @s: string to be returned
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int i = 0;

	s[i];

	while (i > 0)
	{
		if (i != '\0')
		{
			continue;
		}
	}
	return (i);
}
